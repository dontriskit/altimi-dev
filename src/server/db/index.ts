import { env } from "@/env";

/**
 * Database placeholder.
 *
 * Cloudflare D1 via drizzle-orm/d1-http will be configured once
 * the D1 database is provisioned and drizzle-orm is upgraded.
 * For the landing page MVP, no database is needed.
 */
export const db = null;
